//Create the module "ng g module todos --routing"

//- Setup a new Route to the module in app-routing module - path to module
// url name -> 'todos'

//- Create todos-overview component in 'todos' folder 'todos/todos-overview'
//- Make sure component is in declarations of todos.module
//- Setup Route in todos.routing for new component in '' (empty route)

//https://jsonplaceholder.typicode.com/todos
//Figure out DTO (todo.dto.ts) and place in the todos/shared folder
//Make fake DTO array in your todos-overview.component.ts file
//in html Use *ngfor to loop over
// the array and make a li tag pr todo in an ul tag

//https://jsonplaceholder.typicode.com/todos
//Using HTTP Client and Observable (subscribe) show the real data from the API
//change the todos-overview.component.ts to get the data and add it to the todos TodoDto[];
